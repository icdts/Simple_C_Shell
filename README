Simplist C++ shell program.  Use of system() is banned.

Should support:
    & at end to background tasks
    Exiting program at 'exit' or 'quit' command.
    Meaninful error messages at wrong input.
